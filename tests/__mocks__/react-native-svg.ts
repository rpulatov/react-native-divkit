import React from 'react';
const Svg = (props: any) => React.createElement('Svg', props, props.children);
export const Defs = (props: any) => React.createElement('Defs', props, props.children);
export const RadialGradient = (props: any) => React.createElement('RadialGradient', props, props.children);
export const Stop = (props: any) => React.createElement('Stop', props, props.children);
export const Rect = (props: any) => React.createElement('Rect', props, props.children);
export default Svg;
