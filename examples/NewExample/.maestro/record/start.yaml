appId: com.newexample
---
- launchApp:
    clearState: true
    arguments:
      snapshotMode: true
      initialExample: "Start"
- extendedWaitUntil:
    visible:
      id: "divkit-snapshot-area"
    timeout: 60000
- waitForAnimationToEnd
- takeScreenshot:
    path: .maestro/goldens/android/start
    cropOn:
      id: "divkit-snapshot-area"
